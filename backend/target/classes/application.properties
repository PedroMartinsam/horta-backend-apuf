# ===========================
# APUF Backend – Production
# ===========================

spring.application.name=apuf-backend

# ---- Server ----
server.port=${PORT:8080}
server.error.include-message=always

# ---- Database (Render PostgreSQL) ----
spring.datasource.url=postgresql://apuf_db_user:roUpGR3ScfiHcvJVJB5dhpV4wteEQgvi@dpg-d7ns243eo5us73fhu8eg-a.ohio-postgres.render.com/apuf_db
spring.datasource.driver-class-name=org.postgresql.Driver

# (não precisa username/password porque já está na URL)

# ---- JPA / Hibernate ----
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# ---- JWT ----
jwt.secret=apuf-super-secret-2026
jwt.expiration=86400000

# ---- Cloudinary ----
cloudinary.cloud-name=dkbl7ntwt
cloudinary.api-key=221166851988886
cloudinary.api-secret=Oecv7lxrsVW2i4pOEf6zJn9GPxs

# ---- CORS ----
cors.allowed-origins=http://localhost:3000,https://apuf-feira.vercel.app

# ---- Logging ----
logging.level.com.apuf=INFO
logging.level.org.springframework.security=WARN
